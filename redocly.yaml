apis:
  lampac-v1:
    root: docs/specs/openapi-lampac-v1.yaml

extends:
  - recommended

rules:
  security-defined: error
  operation-operationId: warn
  info-license: warn
  tag-description: warn
  no-server-example.com: warn
